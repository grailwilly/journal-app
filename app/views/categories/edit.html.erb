<%= render "categories/top_nav" %>

<div class="container">
<%= link_to image_tag("back-arrow.png"), category_path(@category), class: "back-arrow category-task-back" %>
    <div class="edit-category">
        <h1>Edit Category</h1> 
        <%= render "form", category: @category %>
    </div>
</div>